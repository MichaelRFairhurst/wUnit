import FilePath;
import File;

every TableFileScanner is:

	File[] -- scan($FilePath) {
		var File[] = [];

		foreach($FilePath.getChildren()) {
			if(FilePath.getPath().substr(-6) == ".table") {
				File[].push(File <- FilePath);
			}
		}

		return File[];
	}
