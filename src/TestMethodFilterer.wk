import WakeProperty;
import WakeType;

every TestMethodFilterer is:

	Bool -- Is(WakeProperty)ATestMethod() {
		if(WakeProperty.name.substr(0, 4) == "test") return true;
		if(WakeProperty.name.substr(0, 6) == "~[test") return true;
		if(WakeProperty.name.substr(0, 7) == "~[ test") return true;
		return false;
	}

